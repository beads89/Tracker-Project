 @* localhost:5000/vendors/{id}/orders/new *@
 @* Page to add a new order via fill out form *@
 @* Routed from "/vendors/{id}/orders". Will have two buttons. One for SUBMIT that takes back to /orders and another for CANCEL that takes back to /orders without adding *@